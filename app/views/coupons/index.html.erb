<h1> Index page</h1>

<% @coupons.each do |c|%>
    <%= "Store: #{c.store} allows coupon code #{c.coupon_code}" %> <br>
    <%= link_to "#{c.store} with #{c.coupon_code}", coupon_path(c) %>
<%end%>

<%= link_to 'Make new coupon', new_coupon_path %>